package integration;

import org.junit.Ignore;

@Ignore
public class CasServiceTest {

	/*
	 * It should start a preprogrammed CAS Instance that accepts credentials
	 * when username is equal to password and test the CasServiceImpl
	 */
}
